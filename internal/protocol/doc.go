// Package protocol implements the language-neutral protocol/v1 contract for
// the Go Gateway. Producer implementations must depend on protocol/v1
// artifacts, not on this internal package.
package protocol
